.. highlight:: shell

============
Installation
============


Stable release
--------------

To install Knowledge Graph Query Set, run this command in your terminal:

.. code-block:: console

    $ pip install kgquery

This is the preferred method to install Knowledge Graph Query Set, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for Knowledge Graph Query Set can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/eugeneai/kgquery

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/eugeneai/kgquery/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/eugeneai/kgquery
.. _tarball: https://github.com/eugeneai/kgquery/tarball/master
